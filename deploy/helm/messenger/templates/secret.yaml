apiVersion: v1
kind: Secret
metadata:
  name: {{ include "messenger.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "messenger.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}